# Banner — raw recommendation campaign data (not product-enriched)
# Used for campaigns that return metadata (images, categories, titles) instead of product IDs

type BannerCategoryItem {
    banner_url: String
    banner_app: String
    category: String
    itemId: String
}

type SubCategoryBannerItem {
    firstCategory: String
    secondCategory: String
    subCategoryImage: String
    itemId: String
}

type TextBannerItem {
    title: String
    category: String
    itemId: String
    thumbnail: String
}

type SyneriseBannerData {
    data: [BannerCategoryItem!]
}

type SyneriseBannerSubCategoryData {
    data: [SubCategoryBannerItem!]
}

type SyneriseBannerTextData {
    data: [TextBannerItem!]
}

type SyneriseBannerResult {
    getCategory(
        clientUUID: String!
        campaignId: String!
    ): SyneriseBannerData

    getSubCategories(
        clientUUID: String!
        campaignId: String!
    ): SyneriseBannerSubCategoryData

    getTitles(
        clientUUID: String!
        campaignId: String!
    ): SyneriseBannerTextData
}
