export type LoyaltySignUpProps = {
  title: string;
  description?: string;
  buttonLabel: string;
  formTag: string;
  loyaltyExpressionId: string;
  desiredValue: string;
  termsLabel: string;
  termsUrl?: string;
};
