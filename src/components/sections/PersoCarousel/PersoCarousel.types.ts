export type PersoCarouselTypes = {
  title: string;
  campaignId: string;
  clusterId?: string;
  itemsPerPage: number;
  productCardConfiguration: {
    showDiscountBadge: boolean;
    bordered: boolean;
  };
};
