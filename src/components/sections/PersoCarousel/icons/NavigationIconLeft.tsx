import { Icon } from "@faststore/ui";

const NavigationIconLeft = ({ width = 24, height = 24 }) => {
  return <Icon name="CaretLeft" width={width} height={height} weight="bold" />;
};

export default NavigationIconLeft;
