import { Icon } from "@faststore/ui";

const NavigationIconRight = ({ width = 24, height = 24 }) => {
  return <Icon name="CaretRight" width={width} height={height} weight="bold" />;
};

export default NavigationIconRight;
